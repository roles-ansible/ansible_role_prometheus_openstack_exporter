---
openstack_exporter__version: '1.6.0'
openstack_exporter__releases: "https://github.com/openstack-exporter/openstack-exporter/releases/download/v{{ openstack_exporter__version }}"
openstack_exporter__filename: "openstack-exporter_{{ openstack_exporter__version }}_linux_{{ _arch }}"
openstack_exporter__checksum: "openstack-exporter_{{ openstack_exporter__version }}_checksums.txt"

openstack_exporter__user: 'openstackexporter'
openstack_exporter__group: 'openstackexporter'

openstack_exporter__version_check: true

openstack_exporter__systemd: true

openstack_exporter__clouds:
  clouds: {}

openstack_exporter__cloud_name: 'default'
openstack_exporter__multicloud: false

openstack_exporter__write_clouds_yaml: true
openstack_exporter__port: 9123
openstack_exporter__no_log: true

openstack_exporter__extra_arguments: ''

openstack_exporter__os_user_domain_id: 'default'
openstack_exporter__os_project_domain_id: 'default'
# should we do a version check? (recomended)
submodules_versioncheck: false
